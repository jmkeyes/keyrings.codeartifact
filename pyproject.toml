[build-system]
requires = ["setuptools>=56", "setuptools_scm[toml]>=3.4.1", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
local_scheme = "no-local-version"  # required for PyPI and TestPyPI
